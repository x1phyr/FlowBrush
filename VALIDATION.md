# FlowBrush v1 验证记录

日期：2026-09-08（Asia/Shanghai）。

## 自动化

`npm run typecheck`、`npm run lint`、`npm test`，共 18 项测试：

- 四个轴向、斜向、速度长度上限、柔边强度混合。
- 跟随模式点按无操作；快速长笔画连续；不同事件分段的直线笔画结果一致。
- 平滑的对称性和速度保留；擦除、取消、撤销、重做、清空及历史预算。
- 视口缩放、平移与非方形纹理的坐标换算；缩放保持鼠标下的纹理坐标。
- RG 编码、纵轴反转、行序、静止值和量化误差。
- PNG 签名、IHDR、独立 CRC 校验、Node zlib 解压后逐行像素比对，包含 2048² 多 DEFLATE 块文件。
- 工程浮点字节、底图字段及设置往返；损坏数据、未知版本、NaN 和越界值拒绝。
- WebMCP 工具注册、共享状态写入/回读、非法输入与生命周期清理。

生产构建使用 `npm run build`，生成 Worker 与客户端资产。

## 浏览器交互

Codex 内置浏览器，1440×900 桌面视口；另检查约 586px 宽窄窗口布局。

已验证鼠标拖动绘制、撤销/重做、PNG 底图导入（192×96，等比适配）、导出面板及 Y 分量反转、1024² 和 2048² 工程导入恢复。1024² 工程使用半径 64px 笔刷完成连续拖动画笔，绘制后帧率窗口采样为 52 FPS，向量场 revision 正常递增。损坏工程导入后尺寸、笔刷、revision 与未保存状态不变。

在真实 `document.modelContext` 环境中验证三个 WebMCP 工具的注册与执行；无效半径、越界点产生预期错误，状态保持不变。

WebGL 动态预览正常渲染。上下文丢失/恢复处理已实现，但尚未在浏览器中主动触发 GPU 上下文丢失；也未在各游戏引擎中实机验证 Shader。

文件下载按钮成功进入完成状态，但内置浏览器没有发出可捕获的 download 事件，本次没有把这一点计作“下载文件已落盘”的通过证据。PNG/工程实际字节由自动化编解码测试验证；建议在普通桌面浏览器确认下载落盘。

## 性能

机器：Apple M3 Pro（11 核）、18 GiB RAM、macOS Darwin 25.6.0、Node 22.17.1。`npm run bench` 在每帧移动后更新脏区域 RG 数据，120 次输入，半径 64px。

| 纹理  | 局部编辑 + RG 编码 p95 | PNG 编码 | 工程保存 + 重开 | 历史载荷 |
| ----- | ---------------------: | -------: | --------------: | -------: |
| 1024² |                1.56 ms |  30.6 ms |          306 ms | 4.59 MiB |
| 2048² |                2.20 ms | 117.9 ms |         1073 ms | 9.69 MiB |

这是本机采样，不代表所有设备的性能保证。浏览器 FPS 为开发版窗口采样，不是长期性能追踪。大工程序列化暂在主线程完成，会显示忙碌状态；不做后台 Worker 序列化。

生成本地测试素材：`node --experimental-transform-types tests/make-fixtures.ts`（写入 `/tmp/flowbrush-qa`）。测试素材不进入生产页面。

## 曲线方向修正

将跟随笔刷的相邻事件方向，替换为等距轨迹采样上的对称切线窗口。窗口随笔刷半径变化，限制为 2–24 纹理像素；暂存少量尾部采样点，松手补齐末端。固定方向、平滑、擦除及 PNG / 工程格式保持不变。

新增 7 项回归测试（总计 25 项）：一像素阶梯式 22° 斜线、直角连续过渡、整圆切线与 ±180° 跨界、事件分段一致性、短笔画收尾/取消、急转反向、任意角度笔画的撤销恢复。22° 输入的方向 RMS 抖动小于原始相邻点方向的 10%，有效内段方向误差小于 2°。

同机复测：1024² 局部编辑与 RG 编码 p95 1.45 ms；2048² 为 2.17 ms。已有工程仍可正常打开；修正作用于新画出的跟随笔画，不自动改写已有向量场。
